import { Component, signal } from "@angular/core";

@Component({
    selector: 'clicker-check',
    template: `<div> Click </div>`
})

export class Clicker{
    onClick = () =>{
        //Fill when done with enemy
    }
}